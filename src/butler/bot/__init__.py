"""Discord-facing code: client lifecycle, permissions, slash-command cogs.

The package is named ``bot`` (not ``discord``) to avoid colliding with the
third-party ``discord.py`` package. See PROJECT_SPEC.md §6.
"""
